.screen { width: 100%; height: 100%; }
.dim { background-color: rgba(0,0,0,0.35); }
.countdown { font-size: 120px; -unity-text-align: middle-center; position: absolute; left: 50%; top: 40%; translate: -50% -50%; }
.title  { font-size: 48px; -unity-text-align: middle-center; margin-top: 80px; }
.menu-col { position: absolute; left: 50%; top: 55%; translate: -50% 0; flex-direction: column; padding: 10px; }
