.screen { width: 100%; height: 100%; }
.title { font-size: 40px; margin: 24px; }
.subtitle { font-size: 28px; margin: 16px 24px; }
.row { flex-direction: row; padding: 16px; margin: 2px 8px; }
.cell { font-size: 18px; }
.mono { -unity-font-definition: resource("Fonts/RobotoMono-Regular"); }
.right { -unity-text-align: upper-right; }
.scroll { margin: 0 24px 24px 24px; }
