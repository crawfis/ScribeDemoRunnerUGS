.screen { width: 100%; height: 100%; }
.fill   { background-color: rgba(0,0,0,0); }
.title  { font-size: 48px; -unity-text-align: middle-center; margin-top: 60px; }
.menu-col { flex-direction: column; align-items: center; justify-content: center; padding: 10px; margin-top: 20px; }
.button { min-width: 240px; }
